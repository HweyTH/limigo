// Package config loads and validates Limigo rule configuration.
//
// Config files define request matchers, limiter algorithms, and the
// algorithm-specific parameters selected for each rule.
package config
