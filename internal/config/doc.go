// Package config loads and validates Limigo rule configuration.
//
// Config files define request matchers, rate limits, window durations, and the
// limiter algorithm selected for each rule.
package config
